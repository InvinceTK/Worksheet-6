CHIP A6Q2201823340 {
    IN a, b, c, d;
    OUT f;

    PARTS:
    
    Not(in = a, out = nota);
    And(a = nota, b = b, out = andab);
    Not(in = d, out = notd);
    And(a = andab, b = notd, out = x);


    Not(in = b, out = notb);
    And(a = a, b = notb, out = notab);
    Not(in = c, out = notc);
    And(a = notab, b = notc, out = y);

 
    Or(a = x, b = y, out = f);
}
