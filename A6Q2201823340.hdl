CHIP A6Q2201823340{
    IN a, b, c, d;
    OUT f;

    PARTS:

    Not(in=a,out=nota);
    Not(in=b,out=notb);
    Not(in=c,out=notc);
    Not(in=d,out=notd);

    And(a=a,b=notb,out=andab);
    And(a=andab,b=notc,out=x);

    And(a=nota,b=b,out=andab2);
    And(a=andab2,b=notd,out=y);

    Or(a=x,b=y,out=f);
}
